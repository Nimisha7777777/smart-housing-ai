# AI Service API

## Base URL

/ai

POST /chat

Description

Accepts a natural language request from the authenticated user.

The AI Agent determines which backend service(s) to call based on the user's intent.

The AI Service never directly accesses any database.